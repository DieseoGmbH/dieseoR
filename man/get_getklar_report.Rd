% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_getklar.R
\name{get_getklar_report}
\alias{get_getklar_report}
\title{Fetch a Paginated GetKlar Report}
\usage{
get_getklar_report(
  report,
  shop,
  start_date,
  end_date,
  params = list(),
  end_inclusive = TRUE,
  api_key = .getklar_key(),
  max_pages = 10000L,
  warn_cap = TRUE
)
}
\arguments{
\item{report}{character. Endpunkt ohne Praefix: `revenue-and-profit`,
`attribution`, `attribution-detail` oder `marketing`.}

\item{shop}{character. View-Name oder `shopId`.}

\item{start_date}{character/Date. Startdatum (inklusiv).}

\item{end_date}{character/Date. Enddatum. Standardmaessig **inklusiv**
interpretiert — die API erwartet exklusiv, deshalb wird intern +1 Tag
gerechnet. Mit `end_inclusive = FALSE` wird der Wert unveraendert
durchgereicht.}

\item{params}{list. Zusaetzliche Query-Parameter des jeweiligen Reports.}

\item{end_inclusive}{logical. Siehe `end_date`. Default `TRUE`.}

\item{api_key}{character.}

\item{max_pages}{integer. Sicherheitsnetz gegen Endlosschleifen.}

\item{warn_cap}{logical. Warnen, wenn der serverseitige Zeilen-Cap von
100.000 erreicht wurde (Ergebnis dann abgeschnitten). `FALSE` setzt nur die
rekursive Bisektion, die den Cap selbst aufloest.}
}
\value{
Ein Tibble mit allen Zeilen aller Seiten.
}
\description{
Generischer Zwei-Schritt-Abruf fuer alle Report-Endpunkte der
  Klar Public API: stoesst den Report an, folgt der `dataUrl` und
  anschliessend jeder `nextPage`, bis keine mehr kommt (1.000 Zeilen/Seite).
}
\examples{
\dontrun{
get_getklar_report("revenue-and-profit", "Finance View",
  "2026-06-01", "2026-06-30",
  params = list(dimensions = "calendar_date")
)
}
}
