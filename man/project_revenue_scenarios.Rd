% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_revenue_longrange.R
\name{project_revenue_scenarios}
\alias{project_revenue_scenarios}
\title{project_revenue_scenarios}
\usage{
project_revenue_scenarios(
  base_value = NULL,
  base_year = NULL,
  to_year = 2030,
  scenarios = list(Konservativ = list(g0 = 0.9, g_inf = 0.1, decay = 0.55), Basis =
    list(g0 = 1.28, g_inf = 0.15, decay = 0.7), Optimistisch = list(g0 = 1.28, g_inf =
    0.25, decay = 0.82))
)
}
\arguments{
\item{base_value}{Umsatz des Ankerjahres (€). Default: aus
\code{get_annual_revenue()} letztes volles Jahr.}

\item{base_year}{Ankerjahr. Default: letztes volles Jahr.}

\item{to_year}{Zieljahr. Default \code{2030}.}

\item{scenarios}{Benannte Liste; je Szenario \code{list(g0=, g_inf=, decay=)}.
g0 = Start-YoY, g_inf = langfristige YoY, decay in (0,1) = Abkling-Tempo.
Default: an aktueller ~+128%-Dynamik verankerte konservativ/basis/optimistisch.}
}
\value{
tibble \code{scenario, year, revenue, growth} für alle Projektionsjahre.
}
\description{
Projiziert Jahresumsätze bis \code{to_year} unter Szenarien mit
  geometrisch abklingender YoY-Wachstumsrate:
  \deqn{g_t = g_\infty + (g_0 - g_\infty)\cdot decay^{t-1}}
  Kein statistisches Konfidenzintervall — die Bandbreite bildet
  \emph{Business-Annahmen} ab (Sättigungstempo), nicht Stichprobenrauschen.
}
\details{
Mehrjahres-Umsatzszenarien mit abklingendem Wachstum
}
\examples{
\dontrun{
ann <- get_annual_revenue()
project_revenue_scenarios() # Defaults
}
}
