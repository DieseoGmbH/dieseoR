.Rproj.user
.Rhistory
.RData
.httr-oauth
.DS_Store
.quarto
*.json
.positai
datadir/
*.csv
*.rds
*.png
*.jpg
*.gif
*.tmp
.RData*
.pdf*

# --- ergaenzt 14.09.2026 -------------------------------------------------
# GCP-Service-Account-JSONs. Standen bisher nur in .Rbuildignore (Paket-Build),
# nicht in .gitignore -- in einem OEFFENTLICHEN Repo ein 'git add .' vom Leak
# entfernt. Die *.json-Regel oben greift nicht fuer den ganzen Ordner.
scripts/auth_keys/

# Produktfamilien-Override: einzige CSV, die versioniert gehoert -- sie ist
# Pipeline-Input (build_product_performance.R), kein Datenexport.
!scripts/produktiv/produkt_familien_override.csv
